import PageBlock from "../components/PageBlock";
import {useStoreState} from "easy-peasy";
import {Store} from "../store";

const Home = () => {
	return (
		<PageBlock title={'Micro.Host'}>

		</PageBlock>
	);
};

export default Home;
